using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;

public class ScoreController : MonoBehaviour
{
    private void Sort()
    {
      //  var sortedPlayerList = (from p in PhotonNetwork.PlayerList orderby p.GetMyScore() descending select p).ToList();
    }
}
